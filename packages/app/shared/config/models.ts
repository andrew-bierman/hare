/**
 * AI Models Configuration
 *
 * Re-exports from @hare/config for monorepo consistency.
 */

export {
	AI_MODELS,
	AVAILABLE_MODELS,
	DEFAULT_MODEL_ID,
	getModelById,
	getModelName,
	type AIModel,
	type ModelProvider,
} from '@hare/config'
