'use client'

/**
 * User Entity Hooks
 *
 * Note: OAuth providers hook is in features/auth as it's auth functionality.
 * Import from @hare/app/features/auth instead.
 */

// Re-export types for convenience
export type { OAuthProviders } from '../../../shared/api'
