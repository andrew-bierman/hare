/**
 * Advanced Settings Widget
 *
 * A collapsible panel for fine-tuning LLM configuration values
 * (temperature, max tokens, top-p, top-k).
 */

export { AdvancedSettings, type AdvancedSettingsProps } from './ui/AdvancedSettings'
