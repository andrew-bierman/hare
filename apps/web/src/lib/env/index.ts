/**
 * Environment exports
 *
 * Re-exports from @hare/config package.
 * All environment configuration is centralized in the package.
 */

// Re-export env from @hare/config
export { clientEnv, type ClientEnv, serverEnv, type ServerEnv } from '@hare/config'
