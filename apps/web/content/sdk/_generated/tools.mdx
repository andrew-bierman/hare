---
title: Tools Reference
description: Auto-generated documentation for all available agent tools
---

# Tools Reference

> This documentation is auto-generated from TypeScript source files.

## Classes

## ToolRegistry

Registry for managing and executing tools with type-safe validation.

Centralizes type erasure and input validation. Tools are stored internally
and executed with Zod validation, ensuring type safety at runtime.

### Example

```ts
```ts
const registry = new ToolRegistry()
  .register(kvGetTool)
  .register(httpRequestTool)
  .register(sqlQueryTool)

// Get tool metadata
const tool = registry.get('kv_get')

// Execute with validation
const result = await registry.execute({ id: 'kv_get', params: { key: 'foo' }, context })

// List all tools
const allTools = registry.list()
```
```

### Properties

| Property | Type | Description |
|----------|------|-------------|
| `tools` | `unknown` | - |

### Methods

#### `register(tool: Tool<TInput, TOutput>): this`

Register a tool with the registry.

#### `registerAll(tools: AnyTool[]): this`

Register multiple tools at once.

#### `get(id: string): AnyTool | undefined`

Get tool metadata by ID.
Returns undefined if tool not found.

#### `has(id: string): boolean`

Check if a tool is registered.

#### `list(): AnyTool[]`

List all registered tools (metadata only).

#### `execute(options: {
		id: string
		params: unknown
		context: ToolContext<HareEnv>
	}): Promise<ToolResult<unknown>>`

Execute a tool by ID with input validation.

Validates params against the tool's Zod schema before execution.
Returns a failure result if the tool is not found or validation fails.

## Types

### HareEnvWithSandbox

| Property | Type | Description |
|----------|------|-------------|
| `SANDBOX` | `SandboxBinding` | - |

### SandboxConfig

Cloudflare Sandbox Integration

Uses Cloudflare's official Sandbox SDK for secure code execution.
https://developers.cloudflare.com/sandbox/

Architecture:
- Workers: Your application logic
- Durable Objects: Persistent sandbox instances
- Containers: Isolated Linux environments

Features:
- sandbox.exec() - Execute shell commands
- sandbox.writeFile() / readFile() - File operations
- sandbox.gitCheckout() - Clone git repos
- Each sandbox is fully isolated

Note: Sandbox SDK is in Beta (as of 2025).
For environments without Sandbox SDK, fallback to in-Worker execution.

SECURITY NOTE: These tools have restricted access and include rate limiting,
code validation, and audit logging. Bash execution is disabled by default.

| Property | Type | Description |
|----------|------|-------------|
| `timeout` | `number` | Max execution time in milliseconds |
| `allowNetwork` | `boolean` | Allow network access in sandbox |
| `workDir` | `string` | Working directory in sandbox |

### ToolDatabase

Database abstraction for tool loading.
Implement this interface to integrate with your database.

### LoadAgentToolsInput

Input for loading agent tools.

| Property | Type | Description |
|----------|------|-------------|
| `agentId` | `string` | - |
| `db` | `ToolDatabase` | - |
| `context` | `ToolContext` | - |

### ToolContext

Tool execution context providing access to Cloudflare bindings.
Uses a generic env type to support any Cloudflare binding configuration.

| Property | Type | Description |
|----------|------|-------------|
| `env` | `TEnv` | Cloudflare environment bindings |
| `workspaceId` | `string` | Workspace ID for multi-tenant isolation (optional for SDK users) |
| `userId` (optional) | `string \| null` | User ID for audit trail (optional for anonymous sessions) |

### AnyTool

Type-erased tool metadata interface.

Use this when you need tool metadata without execution capability.
For execution, use `ToolRegistry.execute()` or access the typed `Tool<T>` directly.

| Property | Type | Description |
|----------|------|-------------|
| `id` | `string` | Unique tool identifier |
| `description` | `string` | Human-readable description of what the tool does |
| `inputSchema` | `z.ZodTypeAny` | Zod schema for validating input parameters (type-erased) |
| `outputSchema` | `z.ZodTypeAny` | Zod schema for validating output data (type-erased) |

**Example:**

```ts
```ts
// Get tool metadata from registry
const tool = registry.get('kv_get')
console.log(tool?.id, tool?.description)

// Execute via registry (validates input and output)
const result = await registry.execute({ id: 'kv_get', params: { key: 'foo' }, context })
```
```

### Tool

Fully-typed tool definition for Cloudflare Workers.

Use this interface when defining individual tools for full type safety.
For heterogeneous collections, use `ToolRegistry` which handles type erasure safely.

| Property | Type | Description |
|----------|------|-------------|
| `inputSchema` | `z.ZodType<TInput>` | Zod schema for validating input parameters |
| `outputSchema` | `z.ZodType<TOutput>` | Zod schema for validating output data |
| `execute` | `(params: TInput, context: ToolContext<HareEnv>) => Promise<ToolResult<TOutput>>` | Execute the tool with typed, validated input |

**Example:**

```ts
```ts
const myTool = createTool({
  id: 'search',
  description: 'Search for items',
  inputSchema: z.object({ query: z.string() }),
  execute: async (params, ctx) => {
    // params is typed as { query: string }
    return success({ results: [params.query] })
  }
})

// Direct typed usage
await myTool.execute({ query: 'hello' }, ctx)
```
```

### ToolDefinition

Configuration for creating a tool.

| Property | Type | Description |
|----------|------|-------------|
| `id` | `string` | Unique tool identifier |
| `description` | `string` | Human-readable description of what the tool does |
| `inputSchema` | `z.ZodType<TInput>` | Zod schema for validating input parameters |
| `outputSchema` | `z.ZodType<TOutput>` | Zod schema for validating output data at runtime |
| `execute` | `(params: TInput, context: ToolContext<HareEnv>) => Promise<ToolResult<TOutput>>` | Execute the tool with typed, validated input |

### HareEnv

Cloudflare environment bindings interface.

SDK users should declare their own CloudflareEnv type that matches
their wrangler.toml bindings. This interface shows the expected shape.

Note: This interface doesn't have an index signature so it can be
properly extended by specific CloudflareEnv types generated by wrangler.

| Property | Type | Description |
|----------|------|-------------|
| `AI` (optional) | `Ai` | Workers AI binding - required for AI tools |
| `KV` (optional) | `KVNamespace` | KV namespace - required for KV tools |
| `R2` (optional) | `R2Bucket` | R2 bucket - required for R2 tools |
| `DB` (optional) | `D1Database` | D1 database - required for SQL tools |
| `VECTORIZE` (optional) | `VectorizeIndex` | Vectorize index - required for memory tools |

**Example:**

```ts
```ts
// In your worker project - your Env extends the base HareEnv
interface Env extends HareEnv {
  AI: Ai
  KV: KVNamespace
  R2: R2Bucket
  DB: D1Database
  VECTORIZE: VectorizeIndex
  // Your custom bindings
  MY_CUSTOM_KV: KVNamespace
}
```
```

### AgentControlEnv

Extended environment for agent control tools.
Requires database and optional Durable Object bindings.

| Property | Type | Description |
|----------|------|-------------|
| `DB` | `D1Database` | D1 database - required for agent queries |
| `HARE_AGENT` (optional) | `DurableObjectNamespace` | HareAgent Durable Object namespace - required for agent operations |

### AgentControlContext

Extended context for agent control tools.
Provides access to database for real implementations.

| Property | Type | Description |
|----------|------|-------------|
| `env` | `AgentControlEnv` | - |

## Functions

### `getValidationTools(_context: ToolContext): void`

Get all validation tools

### `getDataTools(_context: ToolContext): void`

Get all data tools.

### `getR2Tools(_context: ToolContext): void`

Get all R2 tools.

### `getIntegrationTools(_context: ToolContext): void`

Get all integration tools.

Zapier tools (for external services via saved integrations) + generic webhook (for custom integrations)

### `getSQLTools(_context: ToolContext): void`

Get all SQL tools.

### `getSearchTools(_context: ToolContext): void`

Get all search tools.

### `getTransformTools(_context: ToolContext): void`

Get all transform tools

### `getHTTPTools(_context: ToolContext): void`

Get all HTTP tools.

### `executeSandboxed({
	code,
	language,
	context,
	config = {},
}: {
	code: string
	language: 'javascript' | 'python' | 'bash'
	context: ToolContext
	config?: Partial<SandboxConfig>
}): Promise<ToolResult<T>>`

Execute code using Cloudflare Sandbox SDK

When SANDBOX binding is available, uses true container isolation.
Otherwise, falls back to in-Worker sandboxed execution.

Security measures:
- Rate limiting per workspace
- Dangerous pattern detection
- Audit logging
- Bash disabled by default (requires explicit opt-in)

### `getSandboxTools(_context: ToolContext): void`

### `getKVTools(_context: ToolContext): void`

Get all KV tools.

### `loadAgentTools(input: LoadAgentToolsInput): Promise<AnyTool[]>`

Load tools attached to an agent from the database.

### `createToolFromConfig({
	config,
	context,
}: {
	config: ToolConfig
	context: ToolContext
}): AnyTool | null`

Create an executable tool from a database configuration.

### `createTool(config: ToolDefinition<TInput, TOutput>): Tool<TInput, TOutput>`

Create a type-safe tool definition with input and output validation.

The tool's execute function is wrapped to validate output data at runtime,
returning a failure if validation fails.

Uses Zod's input type for the execute parameter, allowing optional fields
with defaults to be omitted when calling the tool.

**Example:**

```ts
```ts
const myTool = createTool({
  id: 'my-tool',
  description: 'Does something useful',
  inputSchema: z.object({ query: z.string(), limit: z.number().default(10) }),
  outputSchema: z.object({ result: z.string() }),
  execute: async (params, ctx) => {
    // params.limit is number (default applied by Zod)
    return success({ result: 'done' })
  }
})

// Can call without limit (it has a default)
await myTool.execute({ query: 'hello' }, ctx)
```
```

### `createRegistry(tools: AnyTool[]): ToolRegistry`

Create a new tool registry, optionally pre-populated with tools.

**Example:**

```ts
```ts
const registry = createRegistry([kvGetTool, httpRequestTool])
```
```

### `success(data: T): ToolResult<T>`

Helper to create a successful tool result.

### `failure(error: string): ToolResult<never>`

Helper to create a failed tool result.

### `delegateTo({
	tool,
	params,
	context,
}: {
	tool: Tool<TInput, TOutput>
	params: TInput
	context: ToolContext<HareEnv>
}): Promise<ToolResult<TOutput>>`

Type-safe tool delegation.

Use this when one tool needs to call another tool internally with
known, typed parameters. The input is already typed so no validation
is performed.

**Example:**

```ts
```ts
// In httpGetTool's execute:
execute: async (params, context) => {
  return delegateTo({ tool: httpRequestTool, params: { ...params, method: 'GET' }, context })
}
```
```

### `delegateToWithValidation({
	tool,
	params,
	context,
}: {
	tool: Tool<TInput, TOutput>
	params: unknown
	context: ToolContext<HareEnv>
}): Promise<ToolResult<TOutput>>`

Type-safe delegation with input schema validation.

Use when params come from an untrusted source or when you want
to ensure validation even for internal calls.

**Example:**

```ts
```ts
// When delegating with potentially untrusted params:
execute: async (params, context) => {
  return delegateToWithValidation({ tool: httpRequestTool, params, context })
}
```
```

### `getAITools(_context: ToolContext): void`

Get all AI tools.

### `getUtilityTools(_context: ToolContext): void`

Get all utility tools.

### `getMemoryTools(_context: ToolContext<HareEnv>): void`

Get all memory tools.

### `getSystemTools(context: ToolContext): AnyTool[]`

Get all system tools available to agents.
This includes all built-in tools across all categories.

**Example:**

```ts
```ts
const tools = getSystemTools({
  env: { AI: env.AI, KV: env.KV },
  workspaceId: 'my-workspace',
  userId: 'user-123'
})
```
```

### `getToolsByCategory({
	category,
	context,
}: {
	category: ToolCategory
	context: ToolContext
}): AnyTool[]`

Get tools by category.

**Example:**

```ts
```ts
const aiTools = getToolsByCategory('ai', context)
const storageTools = getToolsByCategory('storage', context)
```
```

### `getSystemToolsMap(context: ToolContext): Map<string, AnyTool>`

Get a map of all system tool IDs to their tools.

### `isSystemTool(toolId: string): toolId is SystemToolId`

Check if a tool ID is a system tool.

### `getAgentControlTools(_context: ToolContext): ExecutableTool[]`

Get agent control tools

## Tools

### validate_email

Validate email addresses. Checks format, common typos, and optionally checks MX records.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `email` | `string` | Email address to validate |
| `checkMx` (optional) | `boolean` | Check if domain has valid MX records |
| `suggestCorrection` (optional) | `unknown` | Suggest corrections for common typos |

### validate_phone

Validate and format phone numbers. Supports international formats.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `phone` | `string` | Phone number to validate |
| `country` (optional) | `unknown` | Expected country code (ISO 3166-1 alpha-2) |
| `format` (optional) | `unknown` | Output format |

### validate_url

Validate URLs. Checks format, protocol, and optionally verifies the URL is reachable.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `string` | URL to validate |
| `allowedProtocols` (optional) | `string` | Allowed protocols |
| `checkReachable` (optional) | `unknown` | Check if URL is reachable (HEAD request) |
| `timeout` (optional) | `number` | Timeout for reachability check |

### validate_credit_card

Validate credit card numbers using the Luhn algorithm. Detects card type (Visa, Mastercard, etc.).

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `number` | `string` | Credit card number to validate |
| `validateExpiry` (optional) | `boolean` | Also validate expiry date |
| `expiryMonth` (optional) | `number` | Expiry month (1-12) |
| `expiryYear` (optional) | `number` | Expiry year (2-digit or 4-digit) |

### validate_ip

Validate IP addresses (IPv4 and IPv6). Detects type and checks for reserved ranges.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `ip` | `string` | IP address to validate |
| `checkType` (optional) | `boolean` | Detect IP type and range |

### validate_json

Validate JSON strings. Provides detailed error messages for invalid JSON.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `json` | `string` | JSON string to validate |
| `strict` (optional) | `unknown` | Strict mode (no trailing commas, no comments) |

### rss

Fetch and parse RSS or Atom feeds. Returns structured feed data with items.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `string` | URL of the RSS/Atom feed |
| `limit` (optional) | `number` | Maximum number of items to return |
| `includeContent` (optional) | `boolean` | Include full content of items |

### scrape

Fetch a web page and extract text content. Can extract specific elements by CSS-like patterns or get clean text.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `string` | URL of the web page to scrape |
| `extract` (optional) | `unknown` | What to extract from the page |
| `selector` (optional) | `unknown` | CSS-like selector pattern to extract specific content |
| `maxLength` (optional) | `number` | Maximum content length to return |
| `timeout` (optional) | `number` | Request timeout in milliseconds |

### regex

Work with regular expressions: test patterns, find matches, extract groups, or replace text.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Regex operation |
| `text` | `string` | Text to operate on |
| `pattern` | `string` | Regular expression pattern |
| `flags` (optional) | `string` | Regex flags (g, i, m, s, u) |
| `replacement` (optional) | `string` | Replacement string for replace operation |
| `groupNames` (optional) | `array` | Names for captured groups in extract |

### crypto

Encrypt or decrypt data using AES-GCM. Generate secure random values.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Crypto operation |
| `data` (optional) | `string` | Data to encrypt/decrypt |
| `key` (optional) | `string` | Base64-encoded encryption key (256-bit) |
| `iv` (optional) | `string` | Base64-encoded initialization vector (for decrypt) |
| `bytes` (optional) | `number` | Number of random bytes to generate |

### json_schema

Validate JSON data against a JSON Schema. Returns validation results and errors.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `data` | `unknown` | Data to validate |
| `schema` (optional) | `string` | JSON Schema to validate against |

### csv

Parse CSV text to JSON or convert JSON arrays to CSV format.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `enum` | CSV operation |
| `data` | `string` | CSV string to parse or array of objects to stringify |
| `options` (optional) | `string` | - |

### template

Render templates with variable substitution. Supports Mustache-like {{variable}} syntax.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `template` | `string` | Template string with {{variable}} placeholders |
| `variables` | `record` | Variables to substitute |
| `options` (optional) | `enum` | - |

### r2_get

Retrieve an object from Cloudflare R2 storage by key. Returns the object content and metadata.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `key` | `string` | The key (path) of the object to retrieve |

### r2_put

Store an object in Cloudflare R2 storage.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `key` | `string` | The key (path) to store the object under |
| `content` | `string` | The content to store |
| `contentType` (optional) | `string` | The MIME type of the content |
| `metadata` (optional) | `string` | Custom metadata to store with the object |

### r2_delete

Delete an object from Cloudflare R2 storage.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `key` | `string` | The key (path) of the object to delete |

### r2_list

List objects in Cloudflare R2 storage with optional prefix filtering.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `prefix` (optional) | `string` | Filter objects by prefix (folder path) |
| `limit` (optional) | `number` | Maximum number of objects to return |
| `cursor` (optional) | `string` | Cursor for pagination |
| `delimiter` (optional) | `unknown` | - |

### r2_head

Get metadata for an R2 object without downloading the content.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `key` | `string` | The key (path) of the object |

### zapier_save

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `unknown` | - |
| `webhookUrl` | `string` | Zapier webhook URL (https://hooks.zapier.com/...) |
| `description` | `unknown` | What does this integration do? |
| `defaultData` (optional) | `string` | Default data to include with every trigger (can be overridden) |

### zapier_list

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `includeStats` (optional) | `unknown` | Include trigger count and last triggered time |

### zapier_trigger

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` (optional) | `string` | - |
| `webhookUrl` (optional) | `unknown` | Direct Zapier webhook URL (use if not using a saved name) |
| `data` (optional) | `string` | Data to send to the Zap |
| `waitForResponse` (optional) | `unknown` | Wait for Zap response (requires Webhooks by Zapier premium) |

### zapier_delete

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `string` | Name of the integration to delete |

### zapier_test

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `string` | Name of the integration to test |
| `testData` (optional) | `string` | Test data to send |

### zapier

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `webhookUrl` | `string` | Zapier webhook URL (https://hooks.zapier.com/...) |
| `data` | `record` | Data to send (available in your Zap) |
| `waitForResponse` (optional) | `unknown` | Wait for Zap response (requires Webhooks by Zapier premium) |

### webhook

Send data to any webhook endpoint with authentication support.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `string` | Webhook URL |
| `method` (optional) | `enum` | - |
| `data` | `unknown` | Request body |
| `headers` (optional) | `record` | Custom headers |
| `auth` (optional) | `enum` | - |
| `timeout` (optional) | `number` | - |

### sql_query

Execute a read-only SQL query on agent data tables. Only SELECT statements on allowed tables are permitted. System tables are not accessible.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `string` | The SQL SELECT query to execute |
| `params` (optional) | `string` | Query parameters for prepared statement |

### sql_execute

Execute a SQL statement that modifies agent data (INSERT, UPDATE, DELETE). Only allowed on agent data tables.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `statement` | `string` | The SQL statement to execute |
| `params` (optional) | `string` | Statement parameters for prepared statement |

### sql_batch

Execute multiple SQL statements in a batch. All statements run in a transaction. Only allowed on agent data tables.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `statements` (optional) | `object` | The SQL statement |

### ai_search

Search through indexed documents and data using AI-powered semantic search. Returns relevant results from your workspace knowledge base.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `string` | The search query |
| `maxResults` (optional) | `number` | Maximum number of results (1-50) |
| `rewriteQuery` (optional) | `unknown` | Optimize query for better retrieval |
| `scoreThreshold` (optional) | `number` | Minimum relevance score (0-1) |

### ai_search_answer

Search through indexed documents and generate an AI answer based on the retrieved context. Best for question-answering. Only searches your workspace data.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `string` | The question or search query |
| `maxResults` (optional) | `number` | Maximum context results (1-50) |
| `systemPrompt` (optional) | `string` | Custom system prompt for the AI response |

### markdown

Parse Markdown to HTML or extract structure (headings, links, images, code blocks).

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Operation to perform |
| `markdown` | `string` | Markdown content to process |
| `options` (optional) | `boolean` | Sanitize HTML output |

### diff

Compare two texts and generate a diff. Shows additions, deletions, and changes.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `original` | `string` | Original text |
| `modified` | `string` | Modified text |
| `mode` (optional) | `unknown` | Comparison mode |
| `context` (optional) | `number` | Lines of context around changes |

### qrcode

Generate QR codes as SVG. Can encode URLs, text, vCards, WiFi credentials, and more.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `data` | `string` | Data to encode in the QR code |
| `type` (optional) | `unknown` | Data type |
| `size` (optional) | `number` | QR code size in pixels |
| `errorCorrection` (optional) | `unknown` | Error correction level |
| `wifiOptions` (optional) | `string` | - |
| `vcardOptions` (optional) | `string` | - |

### compression

Compress or decompress data using gzip or deflate. Works with text or base64 binary data.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `enum` | Operation to perform |
| `data` | `string` | Data to compress/decompress |
| `algorithm` (optional) | `unknown` | Compression algorithm |
| `encoding` (optional) | `unknown` | Input/output encoding |

### color

Convert between color formats (hex, rgb, hsl) and perform color operations (lighten, darken, blend).

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Operation |
| `color` | `string` | Color value (hex, rgb, hsl, or named color) |
| `format` (optional) | `unknown` | Output format |
| `amount` (optional) | `number` | Amount for lighten/darken (0-1) |
| `color2` (optional) | `string` | Second color for blend operation |
| `blendRatio` (optional) | `number` | Blend ratio (0-1) |

### http_request

Make an HTTP request to an external API. Supports GET, POST, PUT, PATCH, DELETE methods.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `string` | The URL to send the request to |
| `method` (optional) | `unknown` | HTTP method |
| `headers` (optional) | `record` | Additional headers to include |
| `body` (optional) | `string` | Request body (for POST, PUT, PATCH) |
| `timeout` (optional) | `number` | Request timeout in milliseconds |

### http_get

Make a simple HTTP GET request.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `string` | The URL to fetch |
| `headers` (optional) | `record` | Additional headers |

### http_post

Make an HTTP POST request with a JSON body.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `url` | `string` | The URL to post to |
| `body` | `unknown` | The JSON body to send |
| `headers` (optional) | `record` | Additional headers |

### code_execute

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `code` | `string` | Code to execute (max 25KB) |
| `language` | `unknown` | Programming language (bash is disabled for security) |
| `timeout` (optional) | `number` | Timeout in ms |

### code_validate

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `code` | `string` | Code to validate |
| `language` | `enum` | - |

### sandbox_file

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `enum` | File operation (delete disabled) |
| `path` | `unknown` | Relative file path in sandbox workspace |
| `content` (optional) | `string` | Content for write |

### kv_get

Retrieve a value from Cloudflare KV storage by key. Returns the stored value or null if not found.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `key` | `string` | The key to retrieve from KV storage |
| `type` (optional) | `unknown` | The type to return the value as |

### kv_put

Store a value in Cloudflare KV storage. Supports optional expiration.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `key` | `string` | The key to store the value under |
| `value` | `string` | The value to store |
| `expirationTtl` (optional) | `number` | Time to live in seconds |
| `metadata` (optional) | `string` | Optional metadata to store with the key |

### kv_delete

Delete a key from Cloudflare KV storage.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `key` | `string` | The key to delete from KV storage |

### kv_list

List keys in Cloudflare KV storage with optional prefix filtering.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `prefix` (optional) | `string` | Filter keys by prefix |
| `limit` (optional) | `number` | Maximum number of keys to return |
| `cursor` (optional) | `string` | Cursor for pagination |

### sentiment

Analyze the sentiment of text. Returns positive, negative, or neutral classification with confidence scores.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `string` | Text to analyze for sentiment |
| `detailed` (optional) | `boolean` | Return detailed emotion breakdown |

### summarize

Generate a concise summary of longer text content. Useful for distilling articles, documents, or conversations.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `string` | Text content to summarize |
| `maxLength` (optional) | `number` | Maximum summary length in words |
| `style` (optional) | `unknown` | Summary style |

### translate

Translate text from one language to another. Supports many common languages.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `string` | Text to translate |
| `targetLanguage` | `unknown` | - |
| `sourceLanguage` (optional) | `unknown` | Source language code (auto-detect if not specified) |

### image_generate

Generate images from text descriptions using AI. Returns the image as base64-encoded data.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `prompt` | `string` | Description of the image to generate |
| `negativePrompt` (optional) | `string` | Things to avoid in the image |
| `width` (optional) | `number` | Image width (256-1024) |
| `height` (optional) | `number` | Image height (256-1024) |
| `steps` (optional) | `number` | Number of diffusion steps (1-50) |
| `guidance` (optional) | `number` | Guidance scale (1-20) |

### classify

Classify text into custom categories. Useful for routing, tagging, or categorizing content.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `string` | Text to classify |
| `categories` | `array` | List of possible categories |
| `multiLabel` (optional) | `unknown` | Allow multiple categories to match |

### ner

Extract named entities from text such as people, organizations, locations, dates, and more.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `string` | Text to analyze for entities |
| `entityTypes` (optional) | `unknown` | Specific entity types to extract (all if not specified) |

### embedding

Generate vector embeddings for text. Useful for semantic search, similarity comparison, and clustering.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `text` | `string` | Text or array of texts to embed |
| `model` (optional) | `unknown` | Embedding model to use |

### question_answer

Answer questions based on provided context. Useful for RAG and document Q&A.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `question` | `string` | Question to answer |
| `context` | `string` | Context/document to find the answer in |
| `options` (optional) | `number` | Maximum answer length |

### datetime

Get current date/time, format dates, parse dates, or calculate time differences. Supports ISO 8601 and common formats.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Operation to perform |
| `date` (optional) | `string` | Date string to operate on (ISO 8601 or common formats) |
| `date2` (optional) | `string` | Second date for diff operation |
| `format` (optional) | `unknown` | - |
| `timezone` (optional) | `unknown` | - |
| `amount` (optional) | `number` | Amount to add/subtract |
| `unit` (optional) | `unknown` | Unit for add/subtract |

### json

Parse JSON strings, stringify objects, extract values using dot notation paths, or transform JSON data.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Operation to perform |
| `data` | `unknown` | The JSON data or string to operate on |
| `path` (optional) | `unknown` | - |
| `value` (optional) | `unknown` | Value to set at path |
| `data2` (optional) | `unknown` | Second object for merge operation |
| `pretty` (optional) | `boolean` | Pretty print output for stringify |

### text

Perform text operations: split, join, replace, case conversion, trim, pad, truncate, word count, and more.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Text operation to perform |
| `text` | `string` | The text to operate on |
| `separator` (optional) | `string` | Separator for split/join |
| `search` (optional) | `string` | String to search for in replace operations |
| `replacement` (optional) | `string` | Replacement string |
| `length` (optional) | `number` | Length for pad/truncate |
| `padChar` (optional) | `string` | Character to pad with |
| `suffix` (optional) | `string` | Suffix for truncate |
| `pattern` (optional) | `string` | Regex pattern for extract |
| `times` (optional) | `number` | Number of times for repeat |

### math

Perform mathematical operations: basic arithmetic, statistics, rounding, random numbers, and more.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Math operation to perform |
| `a` (optional) | `number` | First number |
| `b` (optional) | `number` | Second number |
| `numbers` (optional) | `array` | Array of numbers for aggregate operations |
| `min` (optional) | `number` | Minimum value for random/clamp |
| `max` (optional) | `number` | Maximum value for random/clamp |
| `decimals` (optional) | `number` | Decimal places for rounding |
| `expression` (optional) | `string` | Safe math expression to evaluate |

### uuid

Generate UUIDs (v4), nano IDs, or other unique identifiers.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `type` (optional) | `unknown` | Type of ID to generate |
| `count` (optional) | `number` | Number of IDs to generate |
| `length` (optional) | `number` | Length for nanoid/short |
| `prefix` (optional) | `string` | Optional prefix to add |

### hash

Generate cryptographic hashes (SHA-256, SHA-384, SHA-512) or verify hashes.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` (optional) | `unknown` | Operation to perform |
| `data` | `string` | Data to hash |
| `algorithm` (optional) | `unknown` | Hash algorithm |
| `encoding` (optional) | `enum` | Output encoding |
| `expected` (optional) | `string` | Expected hash for verification |
| `key` (optional) | `string` | Secret key for HMAC |

### base64

Encode data to base64 or decode base64 to text. Supports standard and URL-safe variants.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `enum` | Operation to perform |
| `data` | `string` | Data to encode/decode |
| `urlSafe` (optional) | `boolean` | Use URL-safe base64 variant |

### url

Parse URLs, construct URLs, encode/decode URL components, or manipulate query parameters.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `operation` | `unknown` | Operation to perform |
| `url` (optional) | `string` | URL to operate on |
| `base` (optional) | `string` | Base URL for building/joining |
| `path` (optional) | `string` | Path for building |
| `params` (optional) | `record` | Query parameters |
| `paramNames` (optional) | `array` | Parameter names to remove |
| `text` (optional) | `string` | Text to encode/decode |

### delay

Wait for a specified amount of time before continuing. Useful for rate limiting or timing.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `duration` | `unknown` | Duration to wait in milliseconds (max 30 seconds) |
| `reason` (optional) | `string` | Reason for the delay (for logging) |

### recall_memory

Search your long-term memory for relevant information. Use this to recall facts, context, user preferences, or past conversations that may be helpful.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `query` | `unknown` | What to search for in memory (natural language query) |
| `topK` (optional) | `unknown` | Number of memories to retrieve |
| `type` (optional) | `unknown` | Filter by memory type |
| `tags` (optional) | `array` | Filter by specific tags |

### store_memory

Store important information in long-term memory. Use this to remember facts, user preferences, context, or key points from conversations that should be recalled later.

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `content` | `unknown` | The information to remember. Be specific and include relevant context. |
| `type` (optional) | `unknown` | Category of memory (fact, context, preference, conversation, custom) |
| `tags` (optional) | `array` | Tags for categorization and filtering |
| `source` (optional) | `string` | Source of this information (e.g., conversation ID) |

### agent_list

List all AI agents in the current workspace with their status and configuration

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` (optional) | `unknown` | Filter agents by status |
| `limit` (optional) | `number` | Maximum number of agents to return |

### agent_get

Get detailed information about a specific agent including its state and history

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The unique identifier of the agent |
| `includeHistory` (optional) | `boolean` | Include conversation history |
| `includeTools` (optional) | `boolean` | Include attached tools |

### agent_send_message

Send a message to an agent and receive its response

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The agent to send the message to |
| `message` | `string` | The message content |
| `stream` (optional) | `boolean` | Stream the response |
| `metadata` (optional) | `string` | Additional metadata for the message |

### agent_configure

Update an agent's configuration including instructions, model, and settings

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The agent to configure |
| `name` (optional) | `string` | New name for the agent |
| `description` (optional) | `string` | New description for the agent |
| `instructions` (optional) | `string` | System instructions for the agent |
| `model` (optional) | `string` | AI model to use |
| `config` (optional) | `number` | Model configuration parameters |

### agent_create

Create a new AI agent in the workspace

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `name` | `string` | Name for the new agent |
| `description` (optional) | `string` | Description of the agent |
| `instructions` (optional) | `string` | System instructions |
| `model` (optional) | `unknown` | AI model to use |
| `config` (optional) | `number` | Model configuration |

### agent_delete

Delete an agent from the workspace

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The agent to delete |
| `force` (optional) | `boolean` | Force delete even if agent is deployed |

### agent_schedule

Schedule a task for an agent to execute at a specific time or on a schedule

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The agent to schedule the task for |
| `action` | `string` | The action to perform |
| `executeAt` (optional) | `number` | Unix timestamp to execute at |
| `cron` (optional) | `string` | Cron expression for recurring tasks |
| `payload` (optional) | `record` | Task payload |

### agent_execute_tool

Execute one of an agent's tools directly

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The agent whose tool to execute |
| `toolId` | `string` | The tool to execute |
| `params` | `record` | Tool parameters |

### agent_list_tools

List all tools available to a specific agent

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The agent to list tools for |
| `category` (optional) | `string` | Filter by tool category/type |

### agent_metrics

Get usage metrics and analytics for an agent

**Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `agentId` | `string` | The agent to get metrics for |
| `period` (optional) | `unknown` | Time period for metrics |

